#include <stdio.h>

int a, b, c, i, n;

int main(){
	printf("Ingrese los numeros de la serie de Fibonacci que desee conocer:");
	scanf("%d",&n);
	a = 0;
	b = 1;
	i = 1;

	for(i = 1; i <= n; i++){
		c = a + b;
		a = b;
		b = c;
		printf("%d\n", a);
	}
	return 0;
}
