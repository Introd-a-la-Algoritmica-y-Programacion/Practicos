#include <stdio.h>

int anio;
int bisiesto(int anio);

int main()
{
	printf("\nIngrese un anio para saber si es bisiesto o no: ");
	scanf("%d",&anio);
	bisiesto(anio);
}

int bisiesto(int anio)
{
	if(anio % 4 == 0)
	{
		printf("\nEl anio es bisiesto.\n\n");
	}else{
		printf("\nEl anio no es bisiesto.\n\n");
	}
	return 0;
}
